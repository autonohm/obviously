#include "obgpu/device/DevicePointCloud.h"

namespace obvious { namespace gpu { namespace device { namespace filter {

    __global__ void filter(obvious::gpu::device::PointXyz* input, obvious::gpu::device::PointXyz* output, float* limits)
    {

    }

} // end namespace filter

} // end namespace device

} // end namespace gpu

} // end namespace obvious
